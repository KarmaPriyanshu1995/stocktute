import { en, type MessageKey } from "./en";

/** TODO: human translation. English fallback until reviewed. */
export const hi: Record<MessageKey, string> = { ...en };

export const hiNeedsHumanReview = true;
